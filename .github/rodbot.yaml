on:
  issue_comment:
    - if:
        - command: "test"
        - user_is: ["MEMBER", "OWNER"]
        - user_in: ["petekubiak", "jamessizeland"]
        - is_pr
      steps:
        - run: |
            gh workflow run tests.yaml -R embassy-rs/trouble -F prNr=${{ github.event.issue.number }}
